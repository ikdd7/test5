# 📐 PLAN — 결혼식장 가격 대시보드 ("구글에서 찾는" 좁은 틈 전략)

## 0. 북극성 (모든 UX의 기준)
> **커뮤니티 아님.** 구글에서 "○○지역 웨딩홀 식대"를 검색한 사람에게 **1초 만에 숫자 답**을 주는 대시보드.

70만 회원 다결과 *커뮤니티*로 싸우지 않는다. 다결이 못 닿는 **구글 검색 유입 + 카페 밖 SNS(쇼츠/릴스) 시딩**만 노린다. 두 진입 맥락이 UX 전부를 지배한다:
- **검색 착지(SEO):** 도구가 아니라 "답"이 먼저 떠야 한다.
- **SNS 카드 클릭:** 9:16 그래프 카드가 후킹 → 착지 페이지가 그 이야기를 이어간다.

확정 결정: ① 제보 = **사이트 내장 폼**(Apps Script→시트). ② SEO = **지역별 랜딩 페이지 자동생성**.

---

## 1. 두 사용자 여정
- **A. 검색자:** Google "강남 웨딩홀 식대" → `/지역/강남` 착지 → 화면 위 **중앙값 즉답 + 그래프** → "내 견적 비교" → 호기심 → 제보 1건 → 식장별 상세 해금.
- **B. SNS 클릭:** 인스타/쇼츠 9:16 카드 → 사이트 → 동일 지역 페이지 → 백분위 비교 → 공유/제보.

---

## 2. UX 원칙 7
1. **답 먼저(Answer-first)** — 폼 먼저 금지. 착지 즉시 숫자.
2. **모바일·엄지 우선** — 1열, 큰 탭 타깃, 가로 스크롤 필터, 360px에서 또렷한 차트.
3. **신뢰가 곧 제품** — n수·검증비율·기준연도·갱신일을 항상 노출.
4. **스크린샷 가능** — 핵심 블록은 캡처해도 예쁘게(자발적 공유 유도).
5. **빈 상태 = 제보 유도** — 콜드스타트를 CTA로 전환("5건이면 그래프 열려요").
6. **가짜 데이터 안 보이게** — 실데이터 전까진 그래프 잠금/예시 명시(SEO 신뢰 보호).
7. **감정 훅** — %차이보다 **백분위**("강남 호텔 중 비싼 편 상위 22%").

---

## 3. 정보구조(IA) / 페이지
| 경로 | 역할 | 인덱싱 |
|---|---|---|
| `/` 홈 | 전국 개요 + 지역 선택(데이터 있는 곳 강조) + 비교 진입 | ✅ |
| `/지역/{슬러그}` (예: `/지역/강남`) | **SEO 주력.** H1에 숫자, 홀타입 토글 | ✅ (데이터≥5) |
| `/지역/{슬러그}/{홀타입}` (예: 강남/호텔) | 더 깊은 롱테일 | ✅ (충분할 때만) |
| `/비교` (홈 내 섹션 겸용) | 내 견적 vs 백분위 | ✅ |
| `/방법론`(About) | 출처·중앙값·이상치·검증·법적고지 → E-E-A-T + 신뢰 + 법적 방어 | ✅ |
| `/studio` | 운영자 9:16 카드 스튜디오 | 🚫 noindex |
| `/seed` | 운영자 입력 도구(기존) | 🚫 noindex |

**데이터-게이팅:** 표본 < 5인 지역/타입은 **페이지 생성 안 함**(thin content 패널티 방지) → "데이터 모으는 중 + 제보" 상태로만.

---

## 4. 지역 랜딩 above-the-fold (가장 중요한 화면)
스크롤 없이 보이는 순서:
1. **큰 H1:** "강남 웨딩홀 식대 중앙값 **6.8만원** (1인)"
2. 작은 회색 메타: `n=23 · 검증 14 · 2026 기준 · 6.10 갱신`
3. 한 줄 요약문(데이터→자동생성): "대부분 5.5–7.5만원 · 토요일 낮이 가장 비쌈"
4. 그래프 1개(분포/막대) — 360px에서 또렷
5. CTA: **"내 견적은 평균보다 비쌀까? →"** (앵커 스크롤)

→ **폼 없음.** 상호작용은 그다음. 아래로: 홀타입별·시간대별 중앙값 → 백분위 비교 → 식장별 상세(소프트 게이트) → 방법론·FAQ·면책 → 인근 지역 링크.

```
서울 > 강남                         (breadcrumb)
━━━━━━━━━━━━━━━━━━━━━━━
강남 웨딩홀 식대 중앙값
        6.8만원  (1인)
n=23 · 검증14 · 2026 · 6.10 갱신
대부분 5.5–7.5만 · 토요일 낮 최고가
[ 내 견적 비교하기 → ]
━━━━━━━━━━━━━━━━━━━━━━━
🍽 홀타입별 식대(중앙값)   [막대]
🕐 시간대별 대관료         [막대]
📊 식대 분포 + 내 위치 ▲   [분포]
━━━━━━━━━━━━━━━━━━━━━━━
🏛 실제 식장별 가격  🔒제보 1건 후 해금  [블러]
━━━━━━━━━━━━━━━━━━━━━━━
[제보 30초]   [9:16 카드 공유]
방법론·신뢰 | FAQ | 면책 | 인근: 서초·잠실·분당
```

---

## 5. 핵심 인터랙션
### 5.1 백분위 비교 위젯 (감정 훅)
- 입력: 하객수·식대·(대관료) → 출력: "당신의 식대 7.5만 = 강남 호텔 중 **상위 22%(비싼 편)**"
- 분포 곡선 위에 **내 위치 점(▲)** → 캡처각. %차이보다 백분위 + 위치가 훨씬 강함.
- 결과 → 9:16 공유카드 자동.

### 5.2 내장 제보 폼 (Apps Script → 시트)
- 사이트 안 섹션/모달, 드롭다운 위주 **5필드·30초**.
- 제출 → `fetch` POST → Apps Script(웹앱) → 시트 append(`approved` 비움 = 검토대기).
- 제출 즉시: ① 소프트 게이트 **해금**(localStorage) ② "검토 후 1~2일 내 공개" ③ 공유 유도.
- 방어: 클라 범위·교차필드 검증(기존) + Apps Script 측 재검증 + honeypot + 중복 차단.

### 5.3 소프트 게이트
- 집계·그래프 = **항상 공개**(SEO·공유). "실제 식장별 가격 표"만 제보 1건 후 해금(반투명 블러 → 클릭 시 제보).
- 데이터 < 임계면 게이트 OFF(보여줄 디테일이 없으니).

### 5.4 빈/로딩/에러
- 빈 지역: "강남은 아직 3건. 5건이면 그래프가 열려요. 1건 보태기 →"
- 로딩 스켈레톤 / 시트 실패 시 캐시·예시 조용히 폴백.

---

## 6. SNS 시딩 엔진 (카페 우회의 핵심)
- **9:16 세로 카드 추가**(1080×1920): (a) 결과 카드(내 비교) (b) 인사이트 카드("강남 호텔 식대 분포").
- **`/studio` 카드 스튜디오(운영자):** 지역/컷 선택 → 후킹 헤드라인 자동("잠실 vs 강남 식대 1.4배 차이") → 9:16 PNG 다운로드 → 주 1~2회 인스타·쇼츠 업로드. **이 루프가 다결의 홍보차단을 우회하는 유입 통로.**

---

## 7. SEO 기술
- 페이지별 고유 title/description/**H1(숫자 포함)**.
- 구조화데이터: `Dataset`, `FAQPage`, `BreadcrumbList`.
- 지역 페이지 간 내부링크(강남→서초·잠실·분당).
- 데이터→문장 **자동 요약문**으로 thin content 회피.
- **정적 생성:** `build.js`가 데이터(시트 스냅샷/예시)에서 지역 페이지 HTML + sitemap 생성(SEO엔 정적이 유리). 런타임 fetch는 최신값 갱신용.

---

## 8. 데이터/아키텍처
- 단일 소스 = 구글시트(폼+운영자). 빌드타임에 스냅샷 → 지역별 통계 미리 계산 → 정적 페이지 생성. 런타임 CSV fetch로 점진 갱신.
- 의존성 0 유지(현재 톤). `build.js`는 Node 표준만.
- 콜드스타트: 운영자가 **1~2개 지역(강남/분당) 집중** 입력 → 그 지역만 공개.

---

## 9. 빌드 순서 (데이터 인지)
- **P1 — 신뢰·코어 UX:** `/방법론` 페이지, n·검증·갱신일 노출, 빈상태, 예시 명시 강화.
- **P2 — 지역 랜딩 + 정적 생성기(`build.js`):** answer-first 레이아웃 + 백분위 위젯.
- **P3 — 내장 제보 폼(Apps Script) + 소프트 게이트.**
- **P4 — 9:16 공유카드 + `/studio` 카드 스튜디오.**
- **P5 — SEO 마감:** 스키마·내부링크·sitemap 자동.
- → 운영자 강남/분당 시드 → 공개 → 쇼츠 시딩 루프.

---

## 10. 리스크 / 미해결
- **콜드스타트 여전** — 페이지 많아도 데이터 없으면 빈 껍데기 → 데이터-게이팅으로 "있는 지역만".
- **Apps Script 배포** — 운영자 1회 작업(가이드 제공).
- **정적+런타임 캐시 일관성** — "기준일" 표기로 정직하게.
- **SEO는 수개월 누적** — 즉효 아님(단기 유입은 쇼츠).
- **법적** — `/방법론`+약관+신고로 방어(LEGAL_NOTES 반영).

---

## 11. 이번에 만들 산출물 (승인 시)
P1~P2부터: `index.html` 홈 개편, `방법론.html`, `build.js`(지역 페이지 생성기), 지역 페이지 템플릿, 백분위 비교 위젯, 9:16 카드, `studio.html`, 제보 폼+Apps Script 가이드, 테스트 확장, sitemap 자동화.
