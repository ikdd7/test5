# 💒 결혼식장 비용 투명화 페이지 — 운영 가이드

`wedding.html`은 결혼식장 식대·대관료를 그래프로 공개하는 페이지입니다.
지금은 **예시(합성) 데이터**로 작동하며, 아래 2단계만 하면 **실제 제보 → 자동 반영**이 됩니다. 서버·비용 0.

## 핵심 원칙 (꼭 지키기)
- **실제 식장명+가격을 임의로 지어내 게시하지 않습니다.** 허위사실은 명예훼손 위험이 있어요.
- 데이터는 **사용자 제보**로만 채웁니다. 제보도 "제보자 입력 기준"임을 명시(이미 푸터에 표기).
- 분쟁 소지를 줄이려면 초기엔 **지역·홀타입 단위 집계**로 보여주고, 식장 실명 공개는 제보·검증이 충분히 쌓인 뒤 신중히.

## 1단계 — 제보 받기 (구글폼, 5분)

### 변수 설계 원칙: 폼이 길수록 제보율이 급락 → 3단계로
**[필수 — 30초 컷]**
| 질문 | 형식 | 비고 |
|---|---|---|
| 예식장 이름 | 주관식 | 실명 공개는 제보 3건↑ 쌓인 곳만(아래 규칙) |
| 지역 | 드롭다운 | 서울/경기/인천/부산/대구/대전/광주/제주/기타 |
| 홀 이름·타입 | 주관식+드롭다운 | 같은 식장도 홀마다 가격 다름 |
| 1인 식대 | 숫자(원) | — |
| 부가세·봉사료 포함? | 예/아니오 | ⚠️ 최대 함정. 미포함이면 사이트가 ×1.1 자동 보정 |
| 대관료 | 숫자(원) | 보증인원 충족 면제면 0 |
| 보증인원 | 숫자(명) | — |

**[권장 — 가격을 가르는 변수]**
- 시간대: 토요일 낮 / 토요일 저녁 / 일요일 / 평일 (프라임 여부)
- 예식 시기: 년-월 (성수기 4~5월·9~10월 / 가격은 매년 인상되므로 연도 중요)
- 견적서 사진 첨부(선택) → "검증됨" 배지의 근거

**[선택 — 나중에 욕심낼 것]**
식사 형태(뷔페/코스/한상), 음주류 무제한 여부, 생화/조화, 외부 스냅 출입비, 계약가 vs 실결제가

2. 폼 **보내기 → 링크 복사** → `wedding.html`의 `REPORT_FORM_URL`에 붙여넣기.
   → "제보하기" 버튼이 바로 활성화됩니다.

### 실명 공개 규칙 (분쟁 방지)
- 같은 식장 **제보 3건 이상 + 견적서 사진 1건 이상**일 때만 식장 실명 노출
- 그 전까지는 "서울 강남 컨벤션 A"처럼 익명 집계로만 표시
- 정정 요청 연락처를 푸터에 명시 (분쟁 시 즉시 해당 표본 비공개 처리)

## 2단계 — 제보를 그래프에 자동 반영 (구글시트 CSV)
1. 구글폼 **응답 탭 → 시트로 연결**(스프레드시트 생성).
2. 시트 헤더(첫 행)를 영문 키로 맞추면 가장 안전:
   `venue,region,type,hall,meal,vat,rental,guarantee,slot,month`
   (한글 헤더 `식장명,지역,홀타입,홀,식대,부가세,대관료,보증인원,시간대,시기`도 자동 인식)
   - 필수: region, type, meal / 나머지는 선택(없으면 무시)
   - `vat`에 "미포함"·"별도"가 있으면 식대를 ×1.1 포함가로 자동 보정
   - 시간대는 자유입력해도 "토요일 낮/토요일 저녁/일요일/평일"로 자동 정규화
   - 식대 2만~30만원 밖 값은 이상치로 자동 제외(오타 방지)
3. 시트 메뉴 **파일 → 공유 → 웹에 게시 → 쉼표로 구분된 값(.csv)** 선택 후 게시.
4. 나온 CSV 주소를 `wedding.html`의 `SHEET_CSV_URL`에 붙여넣기.
   → 페이지 접속 시 실제 제보를 불러와 **예시 데이터를 자동 대체**하고, 배지가 "제보 데이터 N건"으로 바뀝니다.

> 불러오기 실패(네트워크/권한) 시 자동으로 예시 데이터로 폴백하므로 화면이 비지 않습니다.

## 콜드스타트(초기 데이터 확보) 팁
가장 큰 난관은 "첫 제보 모으기"입니다. **자발적 제보는 거의 안 들어온다고 가정**하고, 운영자가 초기 100~300건을 직접 채우는 걸 전제로 하세요.

### 초기 시드 입력: `seed.html` (운영자 전용 도구)
- 브라우저로 `seed.html`을 열면 빠른 키인 폼이 뜹니다(색인 제외, noindex).
- 카페·블로그 견적을 읽으며 식장명·식대·대관료 등을 입력 → **Enter로 연속 추가**(지역·타입·시간대는 유지돼 빠름).
- 입력값은 localStorage에 자동 저장(새로고침 안전).
- **📄 CSV 내보내기** → 구글시트에 그대로 붙여넣기(헤더 자동 인식). 또는
- **{ } JSON 내보내기** → `wedding-data.js`를 통째로 교체(부가세 보정·필수필드만 정리됨).

### 수집 출처(출처는 꼭 기록 — 집계용)
1. 네이버 *다이렉트결혼준비* 등 결혼준비 카페의 "○○홀 견적 받아왔어요" 글
2. 블로그 "웨딩홀 견적 비교" 후기, 유튜브 결혼준비 브이로그(견적서 노출분)
3. 웨딩박람회 견적표, 본인·지인 견적
4. 그래프가 의미 있어 보이게 **지역 1~2곳을 먼저 집중**해 채우기(전국 얕게 < 강남 깊게)

### 그다음 — 약한 크라우드소싱(소프트 게이트)
- **집계(평균·그래프)는 항상 무료 공개** → SEO·공유·미끼 역할(이게 죽으면 다 죽음).
- **식장 실명별 상세 견적만** "견적 1건 입력 시 해금"으로 잠그는 걸 권장(하드 월 금지).
- 데이터가 충분(예: 100건+)해진 뒤 게이트를 켜세요. 그 전엔 OFF로 키우기.
- 정적 사이트에선 localStorage 플래그로 해금 처리(우회 가능하나 대부분 안 함, 충분).

## 예시 데이터 다시 만들기
`node wedding-data.gen.js` → `wedding-data.js` 재생성(시드 고정, 재현 가능).

## 🛡️ 데이터 품질(거짓 정보) 방어 — 5겹
가짜·장난 입력이 통계를 망치는 걸 막는 다층 방어입니다. 핵심은 **"입력을 막기"보다 "가짜 1건이 결과를 못 흔들게"**.

1. **입력 증거(검증 등급)** — 견적서 사진이 있으면 `verified` = 검증. 사이트에서 "검증된 제보만 보기" 토글로 분리. 구글폼에 *견적서 사진 업로드* 항목을 넣으세요(가장 강력한 필터).
2. **입력 검증** — `seed.html`은 홀 타입별 통상 범위를 벗어나면 경고(호텔인데 4만원 등), 부가세 미포함 자동 보정.
3. **강건 통계(핵심)** — 표시는 평균이 아닌 **중앙값**. IQR 1.5배 밖 **이상치 자동 제외**. 누가 식대 1억을 넣어도 중앙값은 안 흔들림(실측: 1억 2건 주입 시 평균은 1,343만원으로 폭발해도 중앙값은 11.5만원 유지).
4. **최소 표본** — 같은 조건 제보가 3건 미만이면 통계 대신 "표본 부족" 표시. 단일 제보로 단정하지 않음.
5. **사후 관리(운영자)** — 구글시트에 `approved` 열을 두고 검토한 행만 게시(파서가 무시하도록 미승인 행 비우기), "이 값 이상해요" 신고 채널(폼/이메일) 운영, 분쟁 시 해당 표본 즉시 비공개.

> 구글시트 권장 헤더에 `verified`(또는 `검증`/`견적서`) 열을 추가하면 검증 등급이 자동 반영됩니다. 값 예: `y`, `예`, 사진 URL.

## 수익화
- 상단/중간 광고(애드센스: `ADSENSE_CLIENT`).
- 웨딩 관련 제휴(스드메·예물·청첩장·신혼여행) 배너를 결과 아래 자연스럽게.
