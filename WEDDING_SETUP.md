# 💒 결혼식장 비용 투명화 페이지 — 운영 가이드

`wedding.html`은 결혼식장 식대·대관료를 그래프로 공개하는 페이지입니다.
지금은 **예시(합성) 데이터**로 작동하며, 아래 2단계만 하면 **실제 제보 → 자동 반영**이 됩니다. 서버·비용 0.

## 핵심 원칙 (꼭 지키기)
- **실제 식장명+가격을 임의로 지어내 게시하지 않습니다.** 허위사실은 명예훼손 위험이 있어요.
- 데이터는 **사용자 제보**로만 채웁니다. 제보도 "제보자 입력 기준"임을 명시(이미 푸터에 표기).
- 분쟁 소지를 줄이려면 초기엔 **지역·홀타입 단위 집계**로 보여주고, 식장 실명 공개는 제보·검증이 충분히 쌓인 뒤 신중히.

## 1단계 — 제보 받기 (구글폼, 5분)

### 변수 설계 원칙: 폼이 길수록 제보율이 급락 → 3단계로
**[필수 — 30초 컷]**
| 질문 | 형식 | 비고 |
|---|---|---|
| 예식장 이름 | 주관식 | 실명 공개는 제보 3건↑ 쌓인 곳만(아래 규칙) |
| 지역 | 드롭다운 | 서울/경기/인천/부산/대구/대전/광주/제주/기타 |
| 홀 이름·타입 | 주관식+드롭다운 | 같은 식장도 홀마다 가격 다름 |
| 1인 식대 | 숫자(원) | — |
| 부가세·봉사료 포함? | 예/아니오 | ⚠️ 최대 함정. 미포함이면 사이트가 ×1.1 자동 보정 |
| 대관료 | 숫자(원) | 보증인원 충족 면제면 0 |
| 보증인원 | 숫자(명) | — |

**[권장 — 가격을 가르는 변수]**
- 시간대: 토요일 낮 / 토요일 저녁 / 일요일 / 평일 (프라임 여부)
- 예식 시기: 년-월 (성수기 4~5월·9~10월 / 가격은 매년 인상되므로 연도 중요)
- 견적서 사진 첨부(선택) → "검증됨" 배지의 근거

**[선택 — 나중에 욕심낼 것]**
식사 형태(뷔페/코스/한상), 음주류 무제한 여부, 생화/조화, 외부 스냅 출입비, 계약가 vs 실결제가

2. 폼 **보내기 → 링크 복사** → `wedding.html`의 `REPORT_FORM_URL`에 붙여넣기.
   → "제보하기" 버튼이 바로 활성화됩니다.

### 실명 공개 규칙 (분쟁 방지)
- 같은 식장 **제보 3건 이상 + 견적서 사진 1건 이상**일 때만 식장 실명 노출
- 그 전까지는 "서울 강남 컨벤션 A"처럼 익명 집계로만 표시
- 정정 요청 연락처를 푸터에 명시 (분쟁 시 즉시 해당 표본 비공개 처리)

## 2단계 — 제보를 그래프에 자동 반영 (구글시트 CSV)
1. 구글폼 **응답 탭 → 시트로 연결**(스프레드시트 생성).
2. 시트 헤더(첫 행)를 영문 키로 맞추면 가장 안전:
   `venue,region,type,hall,meal,vat,rental,guarantee,slot,month`
   (한글 헤더 `식장명,지역,홀타입,홀,식대,부가세,대관료,보증인원,시간대,시기`도 자동 인식)
   - 필수: region, type, meal / 나머지는 선택(없으면 무시)
   - `vat`에 "미포함"·"별도"가 있으면 식대를 ×1.1 포함가로 자동 보정
   - 시간대는 자유입력해도 "토요일 낮/토요일 저녁/일요일/평일"로 자동 정규화
   - 식대 2만~30만원 밖 값은 이상치로 자동 제외(오타 방지)
3. 시트 메뉴 **파일 → 공유 → 웹에 게시 → 쉼표로 구분된 값(.csv)** 선택 후 게시.
4. 나온 CSV 주소를 `wedding.html`의 `SHEET_CSV_URL`에 붙여넣기.
   → 페이지 접속 시 실제 제보를 불러와 **예시 데이터를 자동 대체**하고, 배지가 "제보 데이터 N건"으로 바뀝니다.

> 불러오기 실패(네트워크/권한) 시 자동으로 예시 데이터로 폴백하므로 화면이 비지 않습니다.

## 콜드스타트(초기 데이터 확보) 팁
가장 큰 난관은 "첫 제보 모으기"입니다. 추천 순서:
1. 본인·지인 결혼 견적 5~10건 먼저 입력해 그래프가 의미 있어 보이게.
2. **결과 공유 카드(공유훅)** 로 네이버 웨딩 카페·예신 커뮤니티에 "○○지역 호텔 식대 평균 vs 내 견적" 같은 글로 노출 → 제보 유입.
3. "내 지역 데이터 보려면 제보 1건" 식의 가벼운 상호성 메시지.

## 예시 데이터 다시 만들기
`node wedding-data.gen.js` → `wedding-data.js` 재생성(시드 고정, 재현 가능).

## 수익화
- 상단/중간 광고(애드센스: `ADSENSE_CLIENT`).
- 웨딩 관련 제휴(스드메·예물·청첩장·신혼여행) 배너를 결과 아래 자연스럽게.
